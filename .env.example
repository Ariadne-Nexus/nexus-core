# Environment Configuration Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git (it's already in .gitignore)

# ============================================================================
# REQUIRED FOR PRODUCTION MODE
# ============================================================================

# OpenAI API Key
# Get from: https://platform.openai.com/api-keys
# Permissions: API key with sufficient credits for GPT-4 Turbo
# Used by: scripts/daily_v2.py for AI summarization
OPENAI_API_KEY=your-openai-api-key-here

# GitHub Personal Access Token
# Get from: https://github.com/settings/tokens
# Permissions: repo (full control of private repositories)
# Used by: scripts/daily_v2.py for creating issues
GITHUB_TOKEN=your-github-token-here

# Target Repository
# Format: owner/repo (e.g., "dotlink-ops/nextjs")
# Used by: scripts/daily_v2.py to determine where to create issues
REPO_NAME=owner/repo

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Output Directory
# Where automation outputs are saved (JSON files, audit logs)
# Default: ./output (relative to project root)
# OUTPUT_DIR=./output

# Notes Source Directory
# Where input notes are read from (markdown/text files)
# Default: ./output/notes (relative to project root)
# NOTES_SOURCE=./output/notes

# ============================================================================
# SALES PIPELINE CONFIGURATION
# ============================================================================

# Sales Data Source
# Path to sales data file (CSV) or API endpoint
# Example: ./output/sample_sales_data.csv
# SALES_DATA_SOURCE=./output/sample_sales_data.csv

# Sales Data Type
# Type of data source: csv, api, or demo
# Default: demo
# SALES_DATA_TYPE=csv

# ============================================================================
# DEMO MODE
# ============================================================================
# To test without API keys, run with --demo flag:
# python3 scripts/daily_v2.py --demo
#
# Demo mode:
# - Uses realistic sample data
# - No API calls (no costs)
# - Creates output files for testing
# - Perfect for development and demonstrations

# ============================================================================
# VERIFICATION
# ============================================================================
# After setting up .env.local, verify with:
#
# 1. Check file exists and is gitignored:
#    ls -la .env.local
#    git status .env.local  # Should show: ignored
#
# 2. Test automation:
#    source venv/bin/activate
#    python3 scripts/daily_v2.py
#
# 3. Check outputs:
#    cat output/daily_summary.json | jq
